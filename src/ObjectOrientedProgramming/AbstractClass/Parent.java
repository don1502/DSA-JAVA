package ObjectOrientedProgramming.AbstractClass;


public abstract class Parent {
    abstract void career (String name);// If we have an abstract method in the class
                                        // then we to convert the class to abstract class.
    abstract void partner ( String name, int age);

    // Where does abstract classes and methods are used?

    // Use only when we know that child class need to override the parent class else we can use normal class

}
